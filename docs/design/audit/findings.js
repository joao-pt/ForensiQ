/* ForensiQ — Auditoria de design
 *
 * Achados catalogados manualmente a partir dos ficheiros em /source.
 * severity: 'critical' | 'high' | 'medium' | 'low' | 'nit'
 * category: 'Consistência' | 'Acessibilidade' | 'Tipografia' | 'Cor' | 'Espaço' | 'Forense' | 'Mobile' | 'Código' | 'Componentes' | 'Copy'
 */

window.FORENSIQ_AUDIT = {
  summary: {
    files_read: 9,
    pages: 13,
    lines_css: 2900,
    findings_total: 34,
  },

  strengths: [
    "Tokens bem estruturados em <code>main.css</code> — escalas de radius, spacing e tipografia consistentes e com nomenclatura Notion/Linear.",
    "Dois temas (dia/noite) com <code>color-scheme</code> correto e contraste sensível (teal escurecido no claro, off-white tipo Notion).",
    "Acessibilidade base: <code>:focus-visible</code> global, <code>prefers-reduced-motion</code> respeitado, <code>aria-*</code> na navbar e formulários.",
    "Mobile-first pensado — FAB, edge-to-edge em <code>&lt;768px</code>, <code>safe-area-inset-bottom</code> e alvos de toque de 44/52 px.",
    "Uso coerente de <code>JetBrains Mono</code> para dados forenses (hashes, GPS, timestamps, NUIPC).",
    "Linguagem visual sóbria adequada ao contexto institucional — sem slop de gradientes, emoji ou decoração supérflua (com 1 excepção — ver achado #14).",
  ],

  findings: [
    /* ================= CRÍTICOS ================= */
    {
      id: 1,
      severity: "critical",
      category: "Consistência",
      page: "Global",
      title: "Dois sistemas de design paralelos: <code>.btn/.form-input</code> vs <code>.lp-*</code>",
      where: "<code>login.css</code> vs <code>main.css</code>",
      problem: `A página de login define <b>tokens locais próprios</b> (<code>--lp-night</code>, <code>--lp-card-bg</code>, <code>--lp-input-bg</code>, cores hardcoded <code>#05070D</code>, <code>#111624</code>, <code>#0B0F1A</code>) e classes completamente paralelas (<code>.lp-input</code>, <code>.lp-submit</code>, <code>.lp-field</code>) que duplicam o que já existe (<code>.form-input</code>, <code>.btn-primary</code>, <code>.form-group</code>).`,
      impact: `A identidade visual muda entre o login e o resto da app: altura de input (46 px vs 44 px), radius (10 px vs 6 px), cor de fundo (<code>#0B0F1A</code> vs <code>--surface #181B21</code>). Quando mudares o accent amanhã, o login fica para trás. Ignora o theme toggle — <b>o login não tem modo dia</b>.`,
      fix: `Apagar tokens <code>--lp-*</code>. Usar <code>.form-input</code>, <code>.btn-lg.btn-primary</code>. O "selo luminoso" (animação da seta) pode ficar como variante <code>.btn-primary.btn-hero</code>. Painel da constellation mantém-se, mas o cartão deve ser <code>.card</code>.`,
    },
    {
      id: 2,
      severity: "critical",
      category: "Acessibilidade",
      page: "Login",
      title: "Login ignora o tema claro",
      where: "<code>login.css</code> linhas 19–41",
      problem: `Todas as cores do login são hardcoded em hex (<code>#05070D</code>, <code>#111624</code>, <code>#E6E8EE</code>). Não há bloco <code>[data-theme="light"]</code>. Um utilizador que mudou para o tema dia vê o login sempre escuro.`,
      impact: `Inconsistência brutal na primeira impressão, e problema de contraste para utilizadores que dependem do modo claro (luz solar direta no terreno, baixa visão).`,
      fix: `Usar <code>var(--bg)</code>, <code>var(--surface)</code>, <code>var(--text)</code>, etc. Se o login quer mesmo ser "sempre escuro" (decisão válida), força <code>data-theme="dark"</code> no <code>&lt;html&gt;</code> e remove o hardcoded; mas isso <b>tem de ser decisão explícita</b>, documentada.`,
    },
    {
      id: 3,
      severity: "critical",
      category: "Código",
      page: "Global",
      title: "Inline styles em templates — viola a CSP declarada",
      where: "<code>base.html:130</code>, <code>evidences.html:44</code>, <code>occurrences.html:72</code>, <code>evidence_detail.html:65</code>, <code>evidences_new.html</code> (várias), etc.",
      problem: `O próprio comentário no topo de <code>main.css</code> diz "Respeita CSP estrita (sem inline styles nos templates)". Mas há <code>style="padding: 0;"</code>, <code>style="margin: 6px 0;"</code>, <code>style="cursor:pointer;"</code>, <code>style="margin:16px auto;"</code> espalhados pelos templates. A CSP em <code>base.html</code> tem <code>style-src 'self' 'unsafe-inline'</code> — ou seja, a política <b>foi enfraquecida para os acomodar</b>.`,
      impact: `Contradição entre documentação e prática. Perde-se protecção contra XSS injectado no DOM. Um projeto "ISO/IEC 27037" que deixa <code>unsafe-inline</code> na CSP dá péssimo sinal em auditoria de segurança.`,
      fix: `Criar utilities: <code>.p-0</code>, <code>.my-1</code>, <code>.cursor-pointer</code>, <code>.mx-auto</code>. Remover todos os <code>style=""</code>. Endurecer CSP para <code>style-src 'self' https://fonts.googleapis.com</code>.`,
    },

    /* ================= HIGH ================= */
    {
      id: 4,
      severity: "high",
      category: "Consistência",
      page: "Global",
      title: 'Camada "legacy aliases" indica design system em meio redesign',
      where: "<code>main.css:163-191</code>",
      problem: `Existe um bloco explícito "LEGACY ALIASES (remover à medida que cada página é redesenhada)" com ~20 aliases (<code>--primary → --accent</code>, <code>--tint-accent → --warning-tint</code>, <code>--radius → --r-2</code>). <b>Sinal honesto</b>, mas significa que páginas não-redesenhadas (occurrences, evidences, wizards) estão a usar nomes semânticos errados — <code>--tint-accent</code> está mapeado para amarelo (<code>--warning-tint</code>), o que confunde quem lê o código.`,
      impact: `Qualquer alteração futura a <code>--accent</code> não se propaga de forma limpa. O nome <code>--primary</code> sugere "cor principal" mas é só o accent teal. Debug com variáveis vive em labirinto.`,
      fix: `Fazer uma passagem de find/replace em cada CSS de página para resolver os aliases, depois apagar o bloco. Estimar 1–2 horas.`,
    },
    {
      id: 5,
      severity: "high",
      category: "Acessibilidade",
      page: "Global",
      title: "Texto body 15 px + texto hint 12 px — abaixo do recomendado",
      where: "<code>main.css:50-53</code> (<code>--fs-base: 0.9375rem</code>, <code>--fs-xs: 0.75rem</code>)",
      problem: `15 px como base é o "denso à Notion" assumido no comentário — mas Notion tem um público diferente. Para uma <b>app forense usada por First Responders em campo</b> (luz variável, stress, possivelmente luvas), 16 px como base deveria ser o mínimo. E <code>--fs-xs: 12 px</code> é usado em <code>.quick-card-hint</code>, <code>.form-hint</code>, <code>.timeline-date</code>, <code>.wizard-step-label</code>, <code>.lp-version</code>. 12 px em contexto forense/mobile é demasiado pequeno.`,
      impact: `WCAG 1.4.4 (Resize text) ainda passa, mas usabilidade real em campo sofre. Legibilidade no escuro com contraste <code>#9AA0A6</code> sobre <code>#0F1115</code> é aceitável mas não confortável a 12 px.`,
      fix: `<code>--fs-base: 1rem</code> (16 px), <code>--fs-sm: 0.875rem</code> (14 px), <code>--fs-xs: 0.8125rem</code> (13 px). Reservar 12 px apenas para chips e badges compactos.`,
    },
    {
      id: 6,
      severity: "high",
      category: "Consistência",
      page: "Wizards",
      title: "Wizard usa <code>.form-control</code>, resto da app usa <code>.form-input</code>",
      where: "<code>occurrences_new.html</code>, <code>evidences_new.html</code> vs <code>evidences.html</code>",
      problem: `Os inputs nos wizards estão marcados com <code>class="form-control"</code> (convenção Bootstrap); na lista de evidências e em formulários normais, a classe é <code>.form-input</code>. O <code>main.css</code> tem ambas listadas ao mesmo tempo no mesmo seletor CSS, o que mascara o problema mas mantém ambiguidade semântica.`,
      impact: `Confusão para quem entra no codebase: qual é a classe "oficial"? Dificulta lint / procura no código.`,
      fix: `Escolher uma (sugestão: <code>.form-input</code>, é mais específico e consistente com <code>.form-label</code>, <code>.form-group</code>, <code>.form-hint</code>). Renomear nos templates dos wizards.`,
    },
    {
      id: 7,
      severity: "high",
      category: "Forense",
      page: "evidences_new",
      title: "Emojis como ícones de tipo de evidência",
      where: "<code>evidences_new.html:37-56</code>",
      problem: `O selector de tipo de evidência usa emojis: 📱 💾 📄 🖼️ 📦. Os próprios guidelines no topo do <code>main.css</code> dizem "ausência de decoração supérflua". Emojis são renderizados pelo SO — aparecem a cores (iOS Apple Color Emoji), monocromos (Windows segoe), ou quadradinhos.`,
      impact: `Num ecrã pericial, um iPhone mostra um <b>iPhone rosa-choque de 32 px</b> ao lado de tipografia cinza-neutra. Quebra completamente a linguagem visual sóbria. Além disso, a plataforma muda a aparência — inconsistente entre agentes.`,
      fix: `Substituir por SVGs monocromáticos no estilo dos restantes ícones (stroke 1.8, <code>currentColor</code>). Já existem lucide-style ícones em uso pela app — reutilizar.`,
    },
    {
      id: 8,
      severity: "high",
      category: "Componentes",
      page: "Global",
      title: "Cores de estados de custódia distintos em dois sítios",
      where: "<code>custody_timeline.css:194-200</code>",
      problem: `Os 7 estados forenses (APREENDIDA, EM_TRANSPORTE, RECEBIDA_LABORATORIO, EM_PERICIA, CONCLUIDA, DEVOLVIDA, DESTRUIDA) têm cores atribuídas dentro do CSS da timeline. Em <code>evidence_detail.html</code>, <code>dashboard.html</code> e <code>stats.html</code> também se mostram estados, mas <b>não há garantia que as cores coincidam</b> — no detalhe usa-se <code>.badge-accent</code> genérico.`,
      impact: `O mesmo estado "EM_PERICIA" pode aparecer teal na timeline e neutro cinzento na lista — quebra o mapeamento mental do utilizador.`,
      fix: `Criar tokens semânticos de estado: <code>--state-apreendida</code>, <code>--state-em-transporte</code>, etc. Expor como classes <code>.state-pill.state-APREENDIDA</code> reutilizáveis em todos os contextos.`,
    },
    {
      id: 9,
      severity: "high",
      category: "Acessibilidade",
      page: "Dashboard / Listas",
      title: "Skeleton rows não anunciam estado a leitores de ecrã",
      where: "<code>dashboard.html:129-147</code>, <code>evidences.html:47-50</code>, <code>occurrences.html:73-76</code>",
      problem: `Durante loading aparecem <code>.skeleton-row</code> (placeholders shimmer) mas <b>não há</b> <code>aria-busy="true"</code> no container, <code>role="status"</code>, nem texto visualmente escondido "A carregar evidências".`,
      impact: `Utilizadores cegos ouvem <i>nada</i> durante 1–3 s antes de a lista aparecer, sem saberem se a página está congelada ou a carregar.`,
      fix: `<code>&lt;div class="list" id="evidences-list" aria-busy="true" aria-live="polite"&gt;</code> e incluir um <code>&lt;span class="visually-hidden"&gt;A carregar evidências&lt;/span&gt;</code>. Remover <code>aria-busy</code> ao completar.`,
    },
    {
      id: 10,
      severity: "high",
      category: "Consistência",
      page: "Global",
      title: "Duplicação: <code>.info-box</code> vs <code>.callout</code>",
      where: "<code>main.css:942-952</code> vs <code>868-921</code>",
      problem: `Ambos existem: <code>.callout</code> (com variantes info/warning/danger/success) e <code>.info-box</code> (barra lateral colorida, sem variantes). Não há regra clara de quando usar qual. <code>.info-box</code> não aparece em nenhum template que vi.`,
      impact: `Componente morto. Ruído na API do design system.`,
      fix: `Apagar <code>.info-box</code> ou, se há uso válido, documentar variante <code>.callout.bordered</code>.`,
    },
    {
      id: 11,
      severity: "high",
      category: "Mobile",
      page: "occurrences_new (wizard)",
      title: "<code>min-height: 100dvh</code> em falta — wizard corta no Safari móvel",
      where: "<code>wizard.css:14-23</code>",
      problem: `<code>min-height: calc(100vh - 56px - 72px)</code> — <code>100vh</code> no Safari iOS inclui a barra de URL retractável. Quando a barra está visível, a nav do wizard fica <b>por baixo da barra do browser</b>.`,
      impact: `First Responder no terreno toca em "Seguinte" e não encontra o botão. Bloqueador real de usabilidade no iOS (o público-alvo dos First Responders é altamente mobile).`,
      fix: `Adicionar <code>min-height: calc(100dvh - 56px - 72px)</code> como fallback a seguir ao <code>100vh</code>. Idealmente usar <code>svh</code>/<code>dvh</code> directamente.`,
    },
    {
      id: 12,
      severity: "high",
      category: "Acessibilidade",
      page: "Global (toggle tema)",
      title: "Toggle tema não tem <code>aria-pressed</code> ou estado anunciado",
      where: "<code>base.html:58-70</code>",
      problem: `O botão <code>#theme-toggle</code> tem <code>aria-label="Alternar modo dia / noite"</code> mas nada que diga <b>em que modo está actualmente</b>. Também não tem <code>aria-pressed</code>.`,
      impact: `Utilizador de leitor de ecrã toca no botão, ouve "Alternar modo dia / noite, botão", toca — nada confirma a mudança. Tem de adivinhar pela aparência (que não vê).`,
      fix: `Adicionar <code>aria-pressed</code> e actualizar no JS. Ou aria-label dinâmico: "Mudar para modo dia" / "Mudar para modo noite". Anunciar mudança via live region.`,
    },

    /* ================= MEDIUM ================= */
    {
      id: 13,
      severity: "medium",
      category: "Tipografia",
      page: "Global",
      title: "Font-feature-settings activa <code>ss01</code>, <code>ss03</code>, <code>cv11</code> — risco de aparência inesperada",
      where: "<code>main.css:225</code>",
      problem: `<code>font-feature-settings: 'cv11', 'ss01', 'ss03'</code>. <code>ss01</code> no Inter = "Open digits" (1, 4 abertos), <code>ss03</code> = "Curved R", <code>cv11</code> = variante single-storey <code>a</code>. <b>Três alterações simultâneas</b> estilísticas — muda demais em relação ao Inter padrão que a equipa pode assumir.`,
      impact: `Ninguém na equipa percebe porque o Inter "não parece Inter". Aumenta o risco dos números <code>1/4/7</code> renderizarem ligeiramente diferente do que aparece em mockups.`,
      fix: `Reduzir a <code>'cv11'</code> (single-storey <code>a</code>) apenas, que é a única que altera aparência. <code>ss01</code> e <code>ss03</code> são choice estética pouco documentada — tirar ou documentar explicitamente.`,
    },
    {
      id: 14,
      severity: "medium",
      category: "Cor",
      page: "Login",
      title: `Login mistura teal + âmbar num fundo "institucional"`,
      where: "<code>login.css:59-68</code>",
      problem: `A vinheta de fundo combina <code>rgba(45, 212, 191, 0.06)</code> (teal) e <code>rgba(245, 158, 11, 0.04)</code> (warning amber). Amber é a cor <b>semântica de aviso</b> do sistema.`,
      impact: `Usar cor de alerta em ornamentação de login dilui o sinal — quando amarelo aparecer numa toast de aviso, o utilizador já está habituado a vê-lo "só porque sim".`,
      fix: `Usar apenas teal em gradientes decorativos. Amarelo reservado exclusivamente para estados warning.`,
    },
    {
      id: 15,
      severity: "medium",
      category: "Forense",
      page: "evidence_detail / custody_timeline",
      title: "Hash SHA-256 com <code>user-select: all</code> em apenas um sítio",
      where: "<code>custody_timeline.css:77</code> (hash-box)",
      problem: `O <code>.hash-box</code> na evidence header tem <code>user-select: all</code> (click seleciona tudo) — óptimo. Mas <code>.integrity-hash</code> em <code>evidence_detail.html</code> e <code>.timeline-hash</code> na timeline <b>não</b>. Copiar hashes é uma acção forense frequente (verificação externa).`,
      impact: `Inconsistência: em um sítio o hash é clicável-para-copiar, no outro é texto normal que parte quando se tenta selecionar.`,
      fix: `Criar utility <code>.hash</code>: <code>font-family: var(--font-mono); user-select: all; word-break: break-all; cursor: copy;</code> e aplicar em todos os hashes. Considerar botão "copiar" dedicado com feedback visual.`,
    },
    {
      id: 16,
      severity: "medium",
      category: "Componentes",
      page: "Dashboard",
      title: "Quick-card hint truncado com <code>white-space: nowrap</code>",
      where: "<code>dashboard.css:163-168</code>",
      problem: `<code>.quick-card-hint</code> tem <code>overflow: hidden; text-overflow: ellipsis; white-space: nowrap</code>. A frase "Registar cena com GPS e hora do servidor" em mobile 360 px dá 33 chars — <b>vai ser cortada</b> ("Registar cena com GPS e hora do…").`,
      impact: `Informação útil invisível. Num cartão de acção que serve para <i>explicar o que faz</i>, cortar o hint derrota o seu propósito.`,
      fix: `Permitir até 2 linhas: <code>display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical</code>. Ou encurtar os hints para ≤ 30 chars.`,
    },
    {
      id: 17,
      severity: "medium",
      category: "Consistência",
      page: "Custody timeline",
      title: `Sticky sidebar usa <code>top: calc(60px + var(--sp-4))</code> mas navbar mobile é 56 px`,
      where: "<code>custody_timeline.css:18-19</code>",
      problem: `A regra só ativa em <code>&gt;=1024px</code>, onde a navbar é 60 px — ok. Mas o valor está hardcoded. Se alguém alterar a altura da navbar para 64 px, a sidebar fica 4 px desalinhada da navbar e não haverá warning.`,
      impact: `Futuro débito técnico. Alinhamento é assunto de pixel.`,
      fix: `Criar token <code>--navbar-h: 56px</code> e <code>--navbar-h-desktop: 60px</code>. Usar em ambos os sítios.`,
    },
    {
      id: 18,
      severity: "medium",
      category: "Acessibilidade",
      page: "occurrences",
      title: "Segmented control Lista/Mapa — falha ao navegar com teclado",
      where: "<code>occurrences.html:51-68</code>",
      problem: `Os botões têm <code>role="tab"</code>, <code>aria-selected</code>, <code>aria-controls</code> — correto. Mas <b>falta</b> <code>role="tablist"</code> tem que ter gestão explícita de foco (arrow keys entre tabs, apenas o activo em <code>tabindex</code>). Se isso não está implementado no JS (não vi pages/occurrences.js), o componente anuncia-se como tablist mas falha a contrato ARIA.`,
      impact: `Verificadores automáticos passam (atributos ok), mas utilizadores de teclado ficam presos: Tab entra na tab activa, Shift+Tab sai — não há forma de mudar para Mapa sem mouse/touch.`,
      fix: `Implementar pattern ARIA Tabs do APG: <code>roving tabindex</code>, ← → muda tab, Home/End saltam para primeira/última. Ou tornar as duas <code>tabindex="0"</code> (mais simples, menos "correcto" ARIA).`,
    },
    {
      id: 19,
      severity: "medium",
      category: "Cor",
      page: "Global (tema dia)",
      title: "Contraste <code>--text-muted #787774</code> sobre <code>#FAFAF9</code> no limite",
      where: "<code>main.css:149, 138</code>",
      problem: `#787774 sobre #FAFAF9 = ratio ≈ 4.58:1. Passa WCAG AA para texto normal (≥4.5:1), mas falha para pequeno texto (<code>--fs-xs 12 px</code>) em alguns browsers que renderizam Inter mais fino. E falha AAA (7:1).`,
      impact: `Page subtitles, form hints, meta labels ficam no limite da legibilidade ao sol — cenário realista de First Responder.`,
      fix: `Escurecer para <code>#6E6D6A</code> (ratio ≈ 5.6:1). O muted dark (<code>#9AA0A6</code>) está seguro em 6.2:1.`,
    },
    {
      id: 20,
      severity: "medium",
      category: "Copy",
      page: "Vários",
      title: "Mistura de tu/você/imperativo",
      where: "Vários",
      problem: `
      <b>Tu:</b> "Todas as ocorrências a que tens acesso" (dashboard), "Verifica os dados antes de submeter" (wizard), "contacta o responsável" (settings).<br>
      <b>Imperativo neutro:</b> "Selecionar ocorrência" (wizard), "Alternar modo dia / noite" (aria), "Registar transição" (custody).<br>
      <b>Misto:</b> "Pesquisar por descrição ou ocorrência" (placeholder) vs "Acesso restrito a agentes autorizados" (login, frase nominal).`,
      impact: `Percebido como inconsistente. Para utilizadores portugueses: misturar <i>tu</i> com imperativo plural neutro chega a parecer descuido. Em contexto institucional português, <i>tu</i> é informal demais para uma app policial.`,
      fix: `Escolher 1 voz. Recomendação: imperativo neutro ("Verificar dados antes de submeter") ou <i>você</i> explícito ("Verifique os dados"). Evitar <i>tu</i> — soa a app de consumo, não forense.`,
    },
    {
      id: 21,
      severity: "medium",
      category: "Espaço",
      page: "Global",
      title: "Padding lateral do <code>.container</code> desaparece abruptamente em <code>&lt;768px</code>",
      where: "<code>main.css:304-316</code>",
      problem: `Em <code>&gt;=768px</code>: <code>padding: var(--sp-6) var(--sp-6)</code>. Em mobile: <code>padding: 0</code>. E depois cada filho (<code>.page-header</code>, <code>.section-label</code>, <code>.toolbar</code>, <code>.input-group.evidences-search</code>) tem de adicionar manualmente <code>padding: 0 var(--sp-4)</code> para não ficar colado à borda.`,
      impact: `Muito código repetido. Novos componentes esquecem-se e aparecem edge-to-edge não intencional. Padrão frágil.`,
      fix: `Manter <code>padding: 0 var(--sp-4)</code> no container mobile (apenas as <i>cards</i> que queiras full-bleed é que cancelam com <code>margin: 0 calc(var(--sp-4) * -1)</code>). Menos inverter, mais opt-in.`,
    },
    {
      id: 22,
      severity: "medium",
      category: "Componentes",
      page: "Evidence detail",
      title: `"Metadata list" sem estrutura formal de <code>&lt;dl&gt;</code> CSS`,
      where: "<code>evidence_detail.html:66-89</code>",
      problem: `Os templates usam <code>&lt;dl class="meta-list"&gt;&lt;div class="meta-row"&gt;&lt;dt&gt;...&lt;dd&gt;...</code>. Semanticamente correto, mas <b>não encontrei <code>.meta-list</code> nem <code>.meta-row</code> em nenhum CSS global</b>. Só existe em <code>custody_timeline.css</code> uma variante local <code>.evidence-meta</code>.`,
      impact: `A classe é usada em pelo menos 3 páginas (evidence_detail, custody_timeline, settings) mas o estilo só existe num sítio. Aparência inconsistente provável.`,
      fix: `Promover <code>.meta-list</code> a componente no <code>main.css</code>. Documentar.`,
    },
    {
      id: 23,
      severity: "medium",
      category: "Mobile",
      page: "Wizards",
      title: "Nav do wizard desktop tem <code>max-width: 720px</code> — cria cartão flutuante estranho",
      where: "<code>wizard.css:90-100</code>",
      problem: `A nav fixa no fundo em desktop fica com <code>max-width: 720px</code> centrada, com <code>border-radius: var(--r-4) var(--r-4) 0 0</code>. Em viewport 1440 px, fica um cartão de 720 px em baixo, e os lados esquerdo/direito ficam com <code>background: var(--bg)</code> visível <i>por baixo</i> do cartão — parece um popup a sair do fundo.`,
      impact: `Dissonância visual. O wizard é full-width em cima (step content centrado a 640 px) mas o nav é um "cartão" centrado — não bate certo. Em desktop, o botão de "Seguinte" fica a 50% da largura do ecrã por baixo do nada.`,
      fix: `Em desktop, ou (a) full-width com padding elegante, ou (b) limitar também o wizard-step a 720 px para combinar visualmente. Actualmente é layout híbrido confuso.`,
    },
    {
      id: 24,
      severity: "medium",
      category: "Forense",
      page: "custody_timeline",
      title: 'Cor do timeline-dot EM_PERICIA = <code>--accent-hover</code> — não é um "estado"',
      where: "<code>custody_timeline.css:197</code>",
      problem: `<code>.timeline-dot.state-EM_PERICIA { background: var(--accent-hover); }</code> — <code>--accent-hover</code> é um <b>state colour</b> (hover), não uma cor identitária. Usar como cor de estado forense é abusar do token.`,
      impact: `Se amanhã o <code>--accent-hover</code> for refinado (é comum afinar hover), a cor de um <b>estado forense</b> muda sem ninguém se lembrar.`,
      fix: `Criar <code>--state-em-pericia: #14B8A6</code> explícito, ou reutilizar <code>--info</code> / <code>--warning</code> com intenção clara.`,
    },

    /* ================= LOW / NIT ================= */
    {
      id: 25,
      severity: "low",
      category: "Espaço",
      page: "Global",
      title: "Duplicação: <code>--r-2</code> (6 px) e <code>--radius</code> (alias) — três tamanhos visíveis em botões",
      where: "<code>main.css:29</code>",
      problem: `<code>.btn</code> usa <code>--r-2</code> (6 px), <code>.btn-sm</code> também (6 px), <code>.btn-lg</code> também (6 px). Mas <code>.fab</code> usa <code>--r-pill</code>. <code>.wizard-next</code> (botão primary!) usa <code>--r-pill</code>. Inconsistência entre botões primary conforme o contexto.`,
      impact: `Um "confirmar" no wizard (pill) e um "confirmar" numa modal (6 px radius) parecem dois componentes diferentes.`,
      fix: `Decidir: botões no wizard devem usar <code>.btn-primary</code> standard. O design pill nas setas de wizard (back arrow circular) justifica-se, mas o <b>botão Seguinte</b> devia ser o <code>.btn-primary</code>.`,
    },
    {
      id: 26,
      severity: "low",
      category: "Consistência",
      page: "Global",
      title: "Placeholder color hardcoded em fill do SVG do select",
      where: "<code>main.css:647-653</code>",
      problem: `O chevron do <code>&lt;select&gt;</code> usa <code>fill='%239AA0A6'</code> no dark e <code>%23787774</code> no light — cores <b>literais</b> em data-URI, não referenciando o token. Quando <code>--text-muted</code> mudar, a seta não acompanha.`,
      impact: `Débito técnico silencioso.`,
      fix: `Aceitar a duplicação (é difícil reutilizar var em data-URI) mas pôr comentário a ligar ao token <code>--text-muted</code>. Alternativa: usar <code>::after</code> + ícone SVG inline.`,
    },
    {
      id: 27,
      severity: "low",
      category: "Componentes",
      page: "Global",
      title: `.badge border style incoerente entre variantes`,
      where: "<code>main.css:811-818</code>",
      problem: `<code>.badge</code> default tem <code>border: 1px solid var(--border)</code>. Variantes coloridas (<code>.badge-accent</code>, etc.) põem <code>border-color: transparent</code>. <code>.badge-neutral</code> mantém border visível — <b>incoerente</b>.`,
      impact: `Ligeiro. Mas o neutral acaba por parecer mais "pesado" que o accent, o oposto do que se esperaria.`,
      fix: `Escolher: ou todos com hairline, ou todos sem. Preferência: todos sem (usam cor de fundo para separar).`,
    },
    {
      id: 28,
      severity: "low",
      category: "Mobile",
      page: "Login",
      title: "Constellation canvas fica invisível em mobile mas continua a correr JS",
      where: "<code>login.css:80-88</code>, <code>login.html:22</code>",
      problem: `Em <code>&lt;1024px</code>, <code>.lp-chain-panel</code> é <code>display: none</code>. Mas o <code>&lt;canvas id="lp-constellation"&gt;</code> vive dentro desse panel — em princípio não desenha. Valeria confirmar que o JS verifica visibilidade antes de instalar o animation loop (<code>requestAnimationFrame</code>).`,
      impact: `Se não verificar: drena bateria em mobile por uma animação invisível. Também atrasa o primeiro paint.`,
      fix: `No JS de login: <code>if (canvas.offsetWidth === 0) return;</code> antes de começar. Confirmar que <code>prefers-reduced-motion</code> pausa.`,
    },
    {
      id: 29,
      severity: "low",
      category: "Código",
      page: "Global",
      title: "Spinner não é reutilizado nos loading states — há três padrões diferentes",
      where: "<code>evidences.html:47-50</code> vs <code>occurrences.html:73-76</code> vs <code>dashboard.html</code>",
      problem: `Listas usam <code>.loading-overlay &gt; .spinner + &lt;span&gt;A carregar…&lt;/span&gt;</code>. Dashboard usa <code>.skeleton-row</code> (sem spinner). Wizards usam <code>.spinner.mt-4</code> inline. Três abordagens para o mesmo problema.`,
      impact: `Utilizador tem expectativas inconsistentes.`,
      fix: `Decidir: skeletons para listas com row conhecida, spinner para acções ("a submeter"). Aplicar uniformemente.`,
    },
    {
      id: 30,
      severity: "low",
      category: "Acessibilidade",
      page: "Evidences_new (wizard)",
      title: `Tipo de evidência: <code>type-btn.selected</code> não tem <code>aria-pressed</code> nem <code>role="radio"</code>`,
      where: "<code>evidences_new.html:37-56</code>",
      problem: `5 botões <code>&lt;button type="button"&gt;</code> num grupo exclusivo (só um pode estar selecionado). <b>Sem</b> <code>role="radiogroup"</code> no wrapper, sem <code>role="radio"</code>/<code>aria-checked</code> nos botões. O valor selecionado vai para um <code>&lt;input type="hidden"&gt;</code>.`,
      impact: `Utilizador de leitor de ecrã ouve "5 botões" em vez de "grupo de rádio, 5 opções, <i>Documento</i> seleccionado".`,
      fix: `<code>&lt;div role="radiogroup" aria-label="Tipo de evidência"&gt;</code>; <code>&lt;button role="radio" aria-checked="false"&gt;</code>; setas ← → navegam. Ou substituir por <code>&lt;input type="radio"&gt;</code> nativos estilizados.`,
    },
    {
      id: 31,
      severity: "nit",
      category: "Tipografia",
      page: "Global",
      title: "Ordem dos fallbacks Inter — Inter carrega após primeiro paint",
      where: "<code>main.css:44-45</code>, <code>base.html:24</code>",
      problem: `Google Fonts é carregado sem <code>rel="preload"</code> para os ficheiros de .woff2. <code>font-display: swap</code> implícito do Google mostra o fallback (-apple-system) e depois faz flash para Inter. Para uma app onde hash em JetBrains Mono é importante, há FOUT visível.`,
      impact: `"Flicker" de ~200 ms no primeiro load.`,
      fix: `Pré-carregar apenas Inter 400/600 woff2: <code>&lt;link rel="preload" as="font" type="font/woff2" crossorigin href="..."&gt;</code>. Ou auto-host as fontes (são SIL OFL — já documentado).`,
    },
    {
      id: 32,
      severity: "nit",
      category: "Consistência",
      page: "Global",
      title: "Breadcrumb separator é uma barra <code>/</code> texto, não um chevron",
      where: "<code>evidences.html:8</code> e outros",
      problem: `Breadcrumb usa <code>&lt;span class="breadcrumb-sep"&gt;/&lt;/span&gt;</code>. Separador gráfico com barra funciona (Notion usa-o), mas o próprio <code>main.css</code> define <code>.breadcrumb-sep { opacity: 0.4 }</code> sem especificar carácter. A linguagem visual do design system é mais refinada — um chevron em SVG (<code>&gt;</code>) encaixaria melhor.`,
      impact: `Estético. Baixíssima prioridade.`,
      fix: `Opcional. <code>/</code> é legítimo.`,
    },
    {
      id: 33,
      severity: "nit",
      category: "Copy",
      page: "Dashboard",
      title: `"Acções rápidas" como section header é pouco distintivo`,
      where: "<code>dashboard.html:34</code>",
      problem: `"Acções rápidas" é uma label genérica de produto SaaS. Num contexto forense, os atalhos têm identidade funcional (apreender, registar). "Acções rápidas" é ruído.`,
      impact: `Mínimo.`,
      fix: `Considerar remover a label (os cartões falam por si) ou reescrever "Começar agora".`,
    },
    {
      id: 34,
      severity: "nit",
      category: "Código",
      page: "Global",
      title: "Leaflet CSS carregado em duas páginas — duplicado",
      where: "<code>occurrences.html:15-17</code>, <code>occurrence_detail.html:14-16</code>",
      problem: `Cada uma faz o seu próprio <code>&lt;link rel="stylesheet" href=".../leaflet.min.css"&gt;</code>. Ok, é por página — mas se uma terceira página precisar (ex: evidência com mapa), vai ser triplo.`,
      impact: `Marginal (o browser faz cache). Mas <code>integrity</code> hash está duplicado — se mudar a versão, é preciso mudar em dois sítios.`,
      fix: `Extrair para um include <code>{% include "_leaflet_head.html" %}</code>.`,
    },
  ],
};
